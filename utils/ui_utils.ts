import { IconProfile } from "../app/user/[user]/components/info_user";

export const setLinks = ({
  link,
  icon,
}: {
  link: string;
  icon: IconProfile | undefined;
}): Boolean => {
  return true;
};
